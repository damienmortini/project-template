Project Template
----------------

**Project template that uses Compass and Coffeescript**

* Drop your HTML files into the public folder
* Run `setup.sh` to launch Coffeescript/Compass watchers and a simple HTTP Server